
///Find Matches does....
// pub fn find_matches(
//     content: &str,
//     pattern: &str,
//     mut writer: impl std::io::Write,
// ) -> Result<(), anyhow::Error> {
//     for line in content.lines() {
//         if line.contains(pattern) {
//             writeln!(writer, "{}", line)?;
//         }
//     }
//     Ok(())
// }

///Testing Out some documentation stuff AND MORE
pub fn pizza_roo() {
    todo!()
}

//Notes::
//Not sure what's going on but for some reason rustdoc src/"name of crate.rs" works but right now cargo doc does not work, not sure what the issue is but at least I can create documentation.